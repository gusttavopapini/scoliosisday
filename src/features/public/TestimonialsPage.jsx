// src/features/public/TestimonialsPage.jsx
// Rota pública — conteúdo ainda provisório (ver PublicPlaceholder).

import PublicPlaceholder from '../../components/public/PublicPlaceholder.jsx';

export default function TestimonialsPage() {
  return <PublicPlaceholder titleKey="testimonials" />;
}
